{
  "name": "@dozer/react-query-config",
  "version": "0.1.0",
  "description": "Dozer React Query Config",
  "keywords": [
    "dozer",
    "wagmi",
    "config"
  ],
  "homepage": "https://dozer.finance",
  "repository": {
    "type": "git",
    "url": "https://github.com/dozer/dozer.git",
    "directory": "packages/config/react-query"
  },
  "license": "MIT",
  "author": "Dozer",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "source": "src/index.ts",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "TIMING=1 eslint ./**/*.ts* --fix",
    "test": "jest --passWithNoTests"
  },
  "jest": {
    "preset": "@dozer/jest-config/node"
  },
  "dependencies": {    
  },
  "devDependencies": {
    "@dozer/eslint-config": "workspace:*",
    "@dozer/jest-config": "workspace:*",
    "@dozer/prettier-config": "workspace:*",
    "@dozer/typescript-config": "workspace:*",
    "tsup": "6.5.0",
    "typescript": "4.9.4"
  },
  "peerDependencies": {
    "@tanstack/react-query": "4.20.4",
    "@tanstack/react-query-devtools": "4.20.4"
  }
}
